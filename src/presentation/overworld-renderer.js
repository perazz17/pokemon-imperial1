import { TILE_SIZE, drawTile } from './assets/tiles.js';import { drawCharacter, characterPalettes } from './assets/characters.js';import { drawObject } from './assets/objects.js';export class OverworldRenderer{constructor(canvas){this.canvas=canvas;this.ctx=canvas.getContext('2d');this.ctx.imageSmoothingEnabled=false;this.frame=0;}render(game){const s=game.state,map=game.maps[s.map],ctx=this.ctx;this.frame++;this.canvas.width=map.w*TILE_SIZE;this.canvas.height=map.h*TILE_SIZE;map.grid.forEach((row,y)=>row.forEach((kind,x)=>drawTile(ctx,kind,x*TILE_SIZE,y*TILE_SIZE,x,y,{frame:this.frame,theme:map.theme})));map.objects.forEach(o=>drawObject(ctx,o,o.x*TILE_SIZE,o.y*TILE_SIZE));drawCharacter(ctx,s.x*TILE_SIZE,s.y*TILE_SIZE,characterPalettes.player,s.face,this.frame%2===0);}}
